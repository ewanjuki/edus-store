<!DOCTYPE html>
<html>
  <head>
    {{ content_for_header }}
    {{- 'styles.css' | asset_url | stylesheet_tag -}}
  </head>
  <body>
    <div class="container">
      {{ content_for_layout }}
    </div>
    {{- 'theme.js' | asset_url | script_tag -}}
  </body>
</html>
